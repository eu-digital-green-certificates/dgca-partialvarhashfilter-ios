import XCTest
@testable import DGCPartialVarHashFilter

final class DGCPartialVarHashFilterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DGCPartialVarHashFilter().text, "Hello, World!")
    }
}
